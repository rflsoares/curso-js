* Crie um html

* Coloque cada tracinho no html como paragrafo


- Resolva o sistema de equações ax +  by = c e dx + ey =  f em função dos coeficientes de "a" a "f" em função de x e y

- Solução

- y = (af - dc) / (ae - bd)
- x = (c - b((af - dc) / (ae - bd))) / a



Agora crie o script para resolver qualquer tipo de sistema a duas variáveis:

- Crie 06 variáveis (siga a sequencia abaixo) 

// Solicita os coeficientes ao usuário 
let a = parseFloat(prompt("Digite o coeficiente 'a' da primeira equação:"));
let b = parseFloat(prompt("Digite o coeficiente 'b' da primeira equação:"));
... até o f

- Crie as duas váriaveis que calcularão x e y 

// Calcula as soluções
  const y = ???
  const ?? = ???

// Exibe as soluções usando alert
alert("Valor de x: " + x.toFixed(2) + "\nValor de y: " + y.toFixed(2));





Solução com um único prompt

// Solicita os coeficientes ao usuário separados por vírgula
const input = prompt("Digite os 6 coeficientes separados por vírgula (a,b,c,d,e,f):");

// Divide os valores usando a vírgula como separador
const [a, b, c, d, e, f] = input.split(",").map(parseFloat);

// Calcula as soluções
const y = (a * f - d * c) / (a * e - b * d);
const x = (c - b * y) / a;

// Exibe as soluções usando alert
alert(`Valor de x: ${x.toFixed(2)}\nValor de y: ${y.toFixed(2)}`);


